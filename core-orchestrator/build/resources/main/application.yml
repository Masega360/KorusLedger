server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/korus_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  # Magia de Exposed: Crea las tablas si no existen
  exposed:
    generate-ddl: true